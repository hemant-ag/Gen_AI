# Gen-AI

Here we need to run 

python -m venv venv # it will create a virtual env

venv\Scripts\Activate # it will activate the environment

Package installation from requirements.txt

pip install -r requirements.txt

Verify langchain installing by running test.py
